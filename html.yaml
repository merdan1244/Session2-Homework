---
    - name: Apache
      hosts: localhost 
      tasks:
        - name: Installing apache
          yum:
            name: httpd
            state: installed
        - name: Activating to run apache
          systemd: 
            name: httpd
            state: started
        - name: Enabling apache
          systemd:
            name: httpd 
            enabled: on
        
